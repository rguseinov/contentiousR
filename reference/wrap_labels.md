# Wrap long axis labels onto multiple lines

Wrap long axis labels onto multiple lines

## Usage

``` r
wrap_labels(labels, width = 12)
```

## Arguments

- labels:

  Character vector of labels.

- width:

  Target line width in characters. Default `12`.

## Value

`labels`, each wrapped at word boundaries and joined with `\n`.
