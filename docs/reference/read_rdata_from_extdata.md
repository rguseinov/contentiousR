# Load an RData file from package extdata

Load an RData file from package extdata

## Usage

``` r
read_rdata_from_extdata(filename)
```

## Arguments

- filename:

  Character. Name of the `.RData` file located in `inst/extdata`.

## Value

The single object stored in the `.RData` file.
