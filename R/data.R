#' Bundled bibliography for cn_cite()
#'
#' A `bibentry` object with one entry per bundled or interoperable data
#' source, used by [cn_cite()]. Each entry's `keywords` field lists the
#' `contentiousR` function(s) and/or `dataset =` value(s) it documents.
#'
#' @format A `bibentry` object of length 22.
#' @source `data-raw/contentiousR_bib.bib` and `data-raw/contentiousR_bib.R`.
"cn_bib"
