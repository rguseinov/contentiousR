% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_spell_duration.R
\name{add_spell_duration}
\alias{add_spell_duration}
\title{Add conflict spell (peace-years) durations to a state panel}
\usage{
add_spell_duration(panel, event = NULL, ongoing = NULL)
}
\arguments{
\item{panel}{A data frame produced by \code{build_states_panel()}, optionally
already joined with conflict data via \code{\link[=add_conflict]{add_conflict()}}.}

\item{event}{Character. Name of the binary (0/1) column to compute spells
from. If \code{NULL} (the default), \code{add_spell_duration()} looks for exactly
one column in \code{panel} whose name ends in \verb{_onset}, \verb{_incidence}, or
\verb{_ongoing}; if zero or more than one match, it errors and asks for an
explicit \code{event}.}

\item{ongoing}{Logical. If \code{TRUE}, \code{event} is treated as an "ongoing"
series where a run of consecutive \code{1}s is one continuous event (only
the first year of the run counts as a new failure; the remaining years
of that run get \code{NA} in the output, matching
\code{peacesciencer::add_spells()}'s convention of excluding within-event
years from a spell that measures time since the last event). If
\code{FALSE}, every \code{1} in \code{event} is treated as its own event, appropriate
for an already onset-coded column. If \code{NULL} (the default), this is
inferred from the column name: \code{TRUE} for \verb{_incidence}/\verb{_ongoing}
suffixes, \code{FALSE} for \verb{_onset}.}
}
\value{
The input panel with one additional integer column, named after
\code{event} with its onset/incidence/ongoing suffix (if any) replaced by
\verb{_spell}. In a fresh spell, the first observed year is \code{0} and each
subsequent year without a new event increments by one; the event year
itself carries the final count of the spell it closes.
}
\description{
Computes a BTSCS-style spell/duration counter from a binary conflict
column already present in the panel (typically added by
\code{\link[=add_conflict]{add_conflict()}}): the number of years since the last event for that
state, restarting after each new event. This is the same construct as
\code{peacesciencer::add_spells()}, generalized to work with any binary
column rather than a fixed set of bundled column names. Named
\code{add_spell_duration()}, not \code{add_spells()}, to avoid colliding with
\code{peacesciencer::add_spells()} when both packages are attached.
}
\details{
\code{event} must not contain \code{NA}. \code{add_conflict()}'s output frequently has
\code{NA} for country-years unmatched by the source (documented behavior:
absence from a source is not evidence of zero events), and a spell count
cannot be computed across a gap of unknown conflict status. Filter or
otherwise resolve these \code{NA}s before calling \code{add_spell_duration()}.
}
\examples{
panel <- build_states_panel(1990, 2010, coding_system = "gw") |>
  add_conflict(dataset = "ucdp_prio") |>
  tidyr::drop_na(ucdp_prio_onset) |>
  add_spell_duration(event = "ucdp_prio_onset")

}
