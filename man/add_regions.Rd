% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_regions.R
\name{add_regions}
\alias{add_regions}
\title{Add region names to a state panel}
\usage{
add_regions(panel, region = "region")
}
\arguments{
\item{panel}{A data frame produced by \code{build_states_panel()}, containing
a \code{cow} or \code{gw} column.}

\item{region}{Character vector selecting one or more \code{countrycode}
destination classifications to add, from:
\itemize{
\item \code{"region"} (default): World Bank 7-region classification.
\item \code{"region23"}: World Bank's finer, 23-region classification.
\item \code{"un.region.name"}: UN macro-region (continent-level).
\item \code{"un.regionsub.name"}: UN sub-region.
}

See \code{countrycode::countrycode()} for details on these classifications.}
}
\value{
The input panel with one additional column per entry in
\code{region}, named after the classification (e.g. \code{region}, \code{region23}),
holding the corresponding region name for each row (\code{NA} where
\code{countrycode()} finds no match).
}
\description{
Joins region names onto an existing state panel via
\code{countrycode::countrycode()}, mapping the panel's \code{cow}/\code{gw} codes to one
or more of \code{countrycode}'s regional classifications. The coding system is
detected automatically from the panel.
}
\examples{
panel <- build_states_panel(1990, 2000, coding_system = "cow") |>
  add_regions()

panel <- build_states_panel(1990, 2000, coding_system = "cow") |>
  add_regions(region = c("region", "un.regionsub.name"))

}
