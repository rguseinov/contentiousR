% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peacesciencer.R
\name{as_peacesciencer_panel}
\alias{as_peacesciencer_panel}
\title{Make a state panel compatible with peacesciencer}
\usage{
as_peacesciencer_panel(panel)
}
\arguments{
\item{panel}{A state-year data frame containing exactly one of \code{cow} or \code{gw}
and a \code{year} column.}
}
\value{
The input data frame with an additional \code{ccode} (COW) or \code{gwcode}
(Gleditsch-Ward) alias and the attributes \code{ps_system} and
\code{ps_data_type = "state_year"}. Rows and analytical variables are not
changed.
}
\description{
Adds the key alias and metadata expected by state-year functions from the
\code{peacesciencer} package. The original \code{cow} or \code{gw} column is retained, so
the result remains compatible with all \code{contentiousR} functions.
}
\details{
\code{peacesciencer} dispatches its \verb{add_*()} functions using attributes created
by \code{peacesciencer::create_stateyears()}. It also expects its own key names:
\code{ccode} for COW panels and \code{gwcode} for Gleditsch-Ward panels. This adapter
supplies that interface without replacing contentiousR's \code{cow} or \code{gw} key.

Place the adapter immediately before the first \code{peacesciencer} function, or
earlier in the pipeline. Dplyr joins used by contentiousR preserve these
attributes.
}
\examples{
panel <- build_states_panel(1990, 1995, coding_system = "cow") |>
  add_gdp() |>
  as_peacesciencer_panel()

names(panel)
attr(panel, "ps_system")
attr(panel, "ps_data_type")

}
