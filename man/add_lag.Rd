% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_lag.R
\name{add_lag}
\alias{add_lag}
\title{Add lagged variables to a state panel}
\usage{
add_lag(panel, vars, n = 1)
}
\arguments{
\item{panel}{A data frame produced by \code{build_states_panel()}, containing
a \code{cow} or \code{gw} column and a \code{year} column.}

\item{vars}{Character vector of column names in \code{panel} to lag.}

\item{n}{Integer. Number of years to lag by. Default \code{1}.}
}
\value{
The input panel with one additional column per entry in \code{vars},
named \verb{<var>_l}, holding the value of \verb{<var>} from \code{n} years earlier
for the same state (or \code{NA} if that year is not present in \code{panel}).
}
\description{
Adds a lagged version of each requested column, computed within each
state (\code{group_by(cow)} or \code{group_by(gw)}, matching the panel's coding
system) after sorting by year — required for lags to be meaningful in
panel data. Unlike a plain \code{dplyr::lag()}, the lag is \code{NA} wherever the
preceding row is not exactly \code{n} years earlier, so gaps in the year
sequence (e.g. after filtering with \code{tidyr::drop_na()}) do not silently
produce a lag from the wrong year.
}
\examples{
panel <- build_states_panel(1990, 2000, coding_system = "cow") |>
  add_gdp() |>
  add_lag(vars = c("gdp_pcap", "gdp_growth"), n = 1)

}
