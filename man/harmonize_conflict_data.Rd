% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/harmonize_conflict_data.R
\name{harmonize_conflict_data}
\alias{harmonize_conflict_data}
\title{Harmonize externally supplied conflict/event data onto a state panel}
\usage{
harmonize_conflict_data(
  panel,
  data,
  country_col,
  origin_code,
  prefix,
  year_col = NULL,
  date_col = NULL,
  date_format = NULL,
  aggregate = TRUE
)
}
\arguments{
\item{panel}{A data frame produced by \code{build_states_panel()}, containing
a \code{cow} or \code{gw} column and a \code{year} column.}

\item{data}{A data frame of external conflict/event data, one row per
event (the default; see \code{aggregate}).}

\item{country_col}{Character. Name of the column in \code{data} holding
country identifiers.}

\item{origin_code}{Character. The \code{countrycode::countrycode()} origin
code matching \code{country_col}'s format, e.g. \code{"country.name"},
\code{"iso3c"}, \code{"fips"} (GDELT's two-letter country codes), \code{"cown"},
or \code{"gwc"}. See \code{countrycode::codelist} for the full list of
supported origins.}

\item{prefix}{Character. Prefix for the new column(s), e.g. \code{"gdelt"}
produces \code{gdelt_onset}/\code{gdelt_n_events} (see \code{aggregate}).}

\item{year_col}{Character. Name of a column in \code{data} already holding
an integer year. Exactly one of \code{year_col} or \code{date_col} must be
supplied.}

\item{date_col}{Character. Name of a column in \code{data} holding a date to
extract the year from. Exactly one of \code{year_col} or \code{date_col} must
be supplied.}

\item{date_format}{Character. A \code{format} string for \code{\link[=as.Date]{as.Date()}}, e.g.
\code{"\%Y\%m\%d"} for GDELT's \code{SQLDATE} field, or \code{"\%d \%B \%Y"} for ACLED's
\code{event_date}. If \code{NULL} (the default), \code{date_col} is parsed with
\code{as.Date()} directly, which expects ISO 8601 (\code{"\%Y-\%m-\%d"}) or an
already-\code{Date}-typed column. Ignored if \code{year_col} is supplied.}

\item{aggregate}{Logical. If \code{TRUE} (default), \code{data} is treated as one
row per event and is collapsed to one row per country-year, adding
\verb{<prefix>_onset} (always \code{1}, since a row only exists where an event
occurred) and \verb{<prefix>_n_events} (event count that country-year). If
\code{FALSE}, \code{data} is assumed to already be one row per country-year;
every other column in \code{data} (besides \code{country_col}/\code{year_col}/
\code{date_col}) is renamed with \verb{<prefix>_} and joined as-is.}
}
\value{
\code{panel} with the new column(s) joined in via \code{left_join()}.
Country-years absent from \code{data} remain \code{NA} -- as with
\code{\link[=add_conflict]{add_conflict()}}, that is not evidence of zero events, only that
\code{data} doesn't cover that country-year.
}
\description{
Joins a conflict or event dataset that the caller supplies directly --
downloaded via GDELT, ICEWS, ACLED, or any other source not bundled with
\code{contentiousR} -- onto an existing state panel. Unlike \code{\link[=add_conflict]{add_conflict()}},
this function never downloads or bundles any data itself: \code{data} is
whatever data frame the caller already has in R, in whatever raw column
names and country/date encodings that source happens to use.
}
\details{
Country matching uses \code{countrycode::countrycode()}, so any row whose
\code{country_col} value it cannot map to the panel's coding system (an
unrecognized name, a sub-national/organizational entry, etc.) is
dropped, with a message reporting how many rows were dropped.

\code{contentiousR} deliberately does not depend on \code{gdeltr2}, \code{icews}, or
\code{acled.api} -- this function is a generic bridge instead, so the
package is not exposed to any of those clients' own dependencies,
rate limits, or licensing terms. Cite the original data source
directly (this function does not know anything about the specific
provenance of \code{data}).
}
\examples{
panel <- build_states_panel(2015, 2018, coding_system = "cow")

# An ACLED-style extract: one row per event, country as free text.
acled_like <- data.frame(
  country    = c("Nigeria", "Nigeria", "Mali", "Mali", "Mali"),
  event_date = c("2016-03-01", "2017-11-20", "2015-06-14",
                  "2015-09-02", "2018-01-30"),
  stringsAsFactors = FALSE
)

panel |>
  harmonize_conflict_data(
    data        = acled_like,
    country_col = "country",
    origin_code = "country.name",
    date_col    = "event_date",
    prefix      = "acled"
  )

# A GDELT-style extract: FIPS country codes, SQLDATE as YYYYMMDD.
gdelt_like <- data.frame(
  Actor1CountryCode = c("NI", "NI", "ML"),
  SQLDATE           = c(20160301L, 20171120L, 20150614L)
)

panel |>
  harmonize_conflict_data(
    data        = gdelt_like,
    country_col = "Actor1CountryCode",
    origin_code = "fips",
    date_col    = "SQLDATE",
    date_format = "\%Y\%m\%d",
    prefix      = "gdelt"
  )

}
