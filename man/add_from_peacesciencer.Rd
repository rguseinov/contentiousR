% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/peacesciencer.R
\name{add_from_peacesciencer}
\alias{add_from_peacesciencer}
\title{Add data from peacesciencer and restore the contentiousR schema}
\usage{
add_from_peacesciencer(panel, .fun, ...)
}
\arguments{
\item{panel}{A contentiousR state-year data frame containing exactly one of
\code{cow} or \code{gw} and a \code{year} column.}

\item{.fun}{A \code{peacesciencer} function whose first argument is a state-year
data frame, such as \code{peacesciencer::add_archigos}.}

\item{...}{Additional arguments passed to \code{.fun}.}
}
\value{
The data frame returned by \code{.fun}, restored to the input
contentiousR key schema. A pre-existing \code{ccode} or \code{gwcode} column and
pre-existing \code{ps_system} or \code{ps_data_type} attributes are preserved.
}
\description{
Temporarily adapts a contentiousR state-year panel to the column names and
metadata expected by a \code{peacesciencer} \verb{add_*()} function. After that
function returns, the temporary \code{ccode} or \code{gwcode} alias and dispatch
attributes are removed. The original \code{cow} or \code{gw} key is retained.
}
\details{
This helper is intended for \code{peacesciencer} functions that support
state-year data. It does not alter country codes: \code{ccode} is simply
\code{peacesciencer}'s name for the COW code stored as \code{cow} by contentiousR.

Use \code{\link[=as_peacesciencer_panel]{as_peacesciencer_panel()}} instead when several \code{peacesciencer}
functions will be applied in one uninterrupted pipeline and retaining the
compatibility metadata until the end is preferable.
}
\examples{
\dontrun{
library(peacesciencer)

panel <- build_states_panel(1990, 1995, coding_system = "cow") |>
  add_from_peacesciencer(add_archigos)
}

}
