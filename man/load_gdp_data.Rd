% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_gdp_data.R
\name{load_gdp_data}
\alias{load_gdp_data}
\title{Load GDP data}
\source{
\strong{Gapminder:} GDP per capita data from Gapminder. Gapminder data are
distributed under CC BY 4.0; please cite Gapminder and the original data
providers.

\strong{Fariss:} Fariss, C.J., Anders, T., Markowitz, J.N., & Barnum, M.
(2022). New estimates of over 500 years of historic GDP and population
data. \emph{Journal of Conflict Resolution}, 66(3), 553-591.
}
\usage{
load_gdp_data(
  start_year = 1945,
  end_year = 2019,
  dataset = c("gapminder", "fariss"),
  coding_system = c("cow", "gw")
)
}
\arguments{
\item{start_year}{Integer. First year to include.}

\item{end_year}{Integer. Last year to include.}

\item{dataset}{Character. Dataset to load:
\describe{
\item{\code{"gapminder"}}{Gapminder GDP per capita (v32). Coverage:
1945-2019 by default (full source coverage is wider).}
\item{\code{"fariss"}}{Latent GDP and GDP per capita estimates (Fariss,
Anders, Markowitz & Barnum 2022), combined from their \code{fariss_gdp}
and \code{fariss_gdppc} replication files. Coverage: 1500-2019.}
}}

\item{coding_system}{Character. Country coding system to use.
Either \code{"cow"} for Correlates of War codes or \code{"gw"} for
Gleditsch-Ward codes.}
}
\value{
A data frame with country-year GDP indicators. If
\code{coding_system = "cow"}, the data frame contains a \code{cow} column.
If \code{coding_system = "gw"}, it contains a \code{gw} column. Columns differ by
dataset:

\strong{gapminder:} \code{year}, \code{gdp_pcap}, \code{log_gdp_pcap}, \code{gdp_growth}.

\strong{fariss:} \code{year}, \code{fariss_gdp}, \code{fariss_gdppc}.
}
\description{
Loads GDP data from one of two sources included with the package and
returns a country-year dataset using either COW or Gleditsch-Ward country
codes.
}
\details{
\code{gdp_growth} (gapminder only) is \code{NA} unless the preceding row is
genuinely one year earlier for the same country, the same gap-safety
check \code{add_lag()} uses, so a missing year never silently produces a
growth rate computed against the wrong base year.

\code{fariss_gdp} and \code{fariss_gdppc} are the model's own latent-scale
estimates, taken directly from the \code{"latent_gdp"}/\code{"latent_gdppc"} rows of
the replication files (as opposed to the underlying raw indicator series
also present in those files). Their absolute units are not independently
verified here; consult Fariss et al. (2022) before using them outside of
relative/comparative analysis.
}
\examples{
gdp_cow <- load_gdp_data(coding_system = "cow")

gdp_gw <- load_gdp_data(coding_system = "gw")

fariss_gdp <- load_gdp_data(1900, 2015, dataset = "fariss", coding_system = "cow")

}
