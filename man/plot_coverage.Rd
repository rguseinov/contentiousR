% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_coverage.R
\name{plot_coverage}
\alias{plot_coverage}
\title{Plot country-year onset/event coverage of a binary variable}
\usage{
plot_coverage(panel, var, drop_empty = TRUE)
}
\arguments{
\item{panel}{A data frame produced by \code{build_states_panel()}, containing
a \code{cow} or \code{gw} column, a \code{year} column, and \code{var}.}

\item{var}{Character. Name of a binary (0/1) column in \code{panel}, such as
\code{"ucdp_prio_onset"}.}

\item{drop_empty}{Logical. If \code{TRUE} (default), drop states with zero
years where \code{var == 1} before plotting. A state that never has the
event contributes a row with no blue tiles at all, which adds clutter
without showing where/when events happened; set to \code{FALSE} to include
these states anyway.}
}
\value{
A \code{ggplot} object (one tile per country-year, filled by
\code{"Event"} / \code{"No event"}, with unobserved country-years left blank).
States are ordered top-to-bottom by their number of events, most first.
Requires the \code{ggplot2} package.
}
\description{
Draws a country-by-year heatmap of a binary (0/1) event column: \code{1}
years are shaded as an event, \code{0} years as no event, and years where the
state does not appear in \code{panel} at all (e.g. before independence) or
\code{var} is \code{NA} are left blank. Useful for eyeballing when and where
events (e.g. conflict onsets) actually occurred, as opposed to just
where a source has any data.
}
\examples{
if (requireNamespace("ggplot2", quietly = TRUE)) {
  build_states_panel(1990, 2015, coding_system = "cow") |>
    add_conflict("ucdp_prio") |>
    plot_coverage("ucdp_prio_onset")
}

}
